apiVersion: v1
kind: ConfigMap
metadata:
  name: nankion-database-config
  namespace: default
data:
  # Notion database ID for storing financial statements
  DATABASE_ID: ""
  # Database name for reference
  DATABASE_NAME: "nankion-statements"

